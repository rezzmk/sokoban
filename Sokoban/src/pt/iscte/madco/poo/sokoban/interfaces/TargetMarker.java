package pt.iscte.madco.poo.sokoban.interfaces;

public interface TargetMarker {
    /*
     *
     *  -- Marker Interface
     *
     * This interface has nothing in it for a purpose. We don't need to force any sort of specific implementation
     * in this particular specification.
     *
     * A TargetMarker is an object that can tag the targets, which per the specification, is the required criteria to win
     *
     * */
}
